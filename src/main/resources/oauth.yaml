oauth2:
  dev:
    resourceserver:
      jwt:
        issuer-uri: https://auth.yourcompany.com/realms/my-realm

  test:
    resourceserver:
      jwt:
        issuer-uri: https://auth.yourcompany.com/realms/my-realm

  prod:
    resourceserver:
      jwt:
        issuer-uri: https://auth.yourcompany.com/realms/my-realm